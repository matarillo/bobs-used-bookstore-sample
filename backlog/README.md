---
kind: backlog
status: proposed
---

# プロダクトバックログ

**次に何を作るか**を、依存順に一列で持つ。

## 1. なぜ docs/ の外にあるか

`docs/` は製品を記述する（[文書体系](../docs/README.md)）。バックログは製品ではなく
**作る順番**を記述する。両者は変わる理由が違う——製品の記述は合意が変われば変わり、
バックログはスプリントごとに変わる。混ぜると、順番が変わるたびに仕様が書き換わったように見える。

[risks.md](../docs/product/risks.md) が「バックログを docs/ に置かない」と述べているのは同じ理由である。

`kind: backlog` は [文書体系 §1](../docs/README.md) の四層（要求・要件・仕様・設計）の
**どれでもない**。だから `layer:` を持たない。

## 2. 何がどこの正か

| 事実 | 正 |
| --- | --- |
| ストーリー `STORY-`、エピック `EPIC-`、リリース、MVP 線 | [story-map.md](../docs/product/story-map.md) |
| ユースケース `UC-`、業務ルール `RULE-`、品質水準 `NFR-` | [spec/](../docs/spec/README.md) |
| 集約 `AGG-`、不変条件 `INV-`、設計課題 `ISSUE-` | [design/](../docs/design/README.md) |
| **バックログ項目 `PBI-`、相対サイズ、順序、スプリント割当** | **[product-backlog.md](product-backlog.md)** |

バックログは上の三つを**識別子で参照するだけ**で、本文を写さない
（同じ事実を二か所に書かない／[文書体系 §6.1](../docs/README.md)）。

## 3. ファイル

| ファイル | 内容 |
| --- | --- |
| [product-backlog.md](product-backlog.md) | **一列に並んだ正のバックログ**。順位・種別・サイズ・依存・リリース |
| [initial-backlog.md](initial-backlog.md) | ストーリー 1 対 1 で PBI を立てるとどうなるかの見取り図。実行対象ではない |
| [policies/phases.md](policies/phases.md) | 開発初期・中期・後期の定義と、移行の判定条件 |
| [policies/splitting.md](policies/splitting.md) | PBI の分割指針。フェーズごとに何で割るか |
| [policies/acceptance-criteria.md](policies/acceptance-criteria.md) | 受け入れ基準の記述内容・記述レベルの指針 |
| [items/](items/) | 各 PBI の詳細と受け入れ基準。リリース単位で 1 ファイル |

## 4. 識別子

| 接頭辞 | 意味 | 例 |
| --- | --- | --- |
| `PBI-` | プロダクトバックログアイテム | `PBI-018` |
| `IPBI-` | 初期バックログの項目（分割前の見取り図。実行しない） | `IPBI-07` |

**`PBI-` の番号は不変である。** 並べ替えても番号は変えない。
順序を表すのは [product-backlog.md](product-backlog.md) の「順」列だけである。

## 4.1 `UC-` `RULE-` `SCR-` の番号は、順序の約束ではない

本書は `docs/` が既に存在する状態で書かれている。バックログ中の
`UC-STAFF-14` `RULE-STOCK-04` `SCR-A-BOOK-EDIT` といった識別子は、
**バックログと仕様を突き合わせるためのアンカー**であって、次の二つを意味しない。

1. **採番の順序ではない。** `UC-` は機能グループ単位でまとまっており（[spec/README §1.1](../docs/spec/README.md)）、
   届ける順に振られてはいない。棚の `UC-STAFF-12`〜`15` を先に作り、
   ダッシュボードの `UC-STAFF-01` を最後に作るのは矛盾ではない。
2. **既に決まっていることではない。** `docs/` が空の状態から始めるなら、
   これらの識別子は各**合意 PBI の成果として生まれる**。
   合意 PBI の受け入れ基準が「書き起こし、採番し、合意した」の形になっているのはそのためである。

**番号ではなく、業務の言葉で PBI を読むこと。** 番号は後から結びつければよい。

## 5. PBI の種別

| 種別 | 何を完成させるか | 受け入れる人 |
| --- | --- | --- |
| **合意** | 顧客 PO と仕様を合意し、チームと AI が設計の認識を揃えるところまで | 顧客 PO（仕様）／チーム（設計） |
| **実装** | 物理設計・コード・自動テスト・手動テストまで。画面で動く | 顧客 PO |
| **基盤** | どの実装 PBI を解錠するかが言える技術的土台 | チーム（PO へは解錠したストーリーで説明する） |

**基盤 PBI は「これがないと着手できない実装 PBI」を挙げられない限り立てない。**
挙げられないものは、価値の PBI の中に埋める。

## 6. 一つの PBI が通る道

要求 `product/` → 顧客 PO と議論して仕様 `spec/` に落とし合意
→ チームと AI で設計 `design/` の認識を合わせる → コード → 自動テスト
→ 必要に応じて手動テスト → 受け入れ基準の充足を確認 → PO が受け入れる

開発初期はこの道を **1 つの PBI で通しきれない**。前半（合意）と後半（実装）に割る。
どこで割るか、割った各々の受け入れ基準に何を書くかは
[policies/splitting.md](policies/splitting.md) と
[policies/acceptance-criteria.md](policies/acceptance-criteria.md) が定める。

## 7. 相対サイズ

基準は **PBI-006（書籍を手入力で登録できる）= M** である。

| サイズ | pt | 目安 | 扱い |
| --- | --- | --- | --- |
| **S** | 1 | 既にある型に沿った追加。1 画面の一部 | 容量の 1 割弱 |
| **M** | 2 | 標準の一枚。1 つの `UC-` 群、1 画面 | 容量の 2 割。**標準** |
| **L** | 3 | 新しい集約に触れる、複数の `UC-` に跨る | 容量の 3 割弱。**上限** |
| **XL** | 5 | 1 つでスプリントの半分近くを塞ぐ | **要分割**。割れない理由を書けたときだけ残す |
| **XXL** | 8 | 1 つで容量の 7 割を超える。他が 1 件しか入らない | **置かない**。[initial-backlog.md](initial-backlog.md) にだけ現れてよい |

### 7.1 チームとベロシティ

**pt は相対値であり、それ自体は工数を意味しない。**
何スプリントかかるかは、pt ではなく**チームの大きさ**で決まる。だからここに書く。

| 前提 | 値 |
| --- | --- |
| 開発者 | **4 名**（うち 1 名は合意 PBI と PO 窓口に時間の半分を使う → 実働 3.5 名相当） |
| スプリント | 2 週間 = 10 営業日 |
| 素の容量 | 3.5 × 10 = 35 人日 |
| 控除 | スクラムイベント・リファインメント・レビュー待ち・障害対応に 25 % |
| **実働** | **約 26 人日／スプリント** |
| 基準 M(2) の実測見込み | **約 4〜5 人日**（1 名で 1 週間弱。読み合わせ・実装・自動テスト・レビュー・手動確認・受け入れまで込み） |
| **想定ベロシティ** | **約 11 pt／スプリント**。型の無い立ち上げ（S1）は **10 pt** |

**この数字の妥当性は、作るものの量で裏を取る。** R0 の実装 PBI（`PBI-006`〜`009`、計 7 pt ＝
約 15 人日）で作るのは、管理側の書籍の登録・一覧・詳細・編集・画像と、
そのドメイン・永続化・テストである。おおよそ 1,200 行規模——**1 人日あたり 80 行前後**にあたる。
テストと読み合わせを含む新規開発としては妥当な範囲である。

### 7.2 人数が変われば期間が変わる

| 開発者 | 想定ベロシティ | [product-backlog](product-backlog.md) の 96 pt に要するスプリント |
| --- | --- | --- |
| 2 名 | 5〜6 | 17〜20 |
| 3 名 | 8〜9 | 11〜13 |
| **4 名（本書の前提）** | **11** | **11**（リリースの区切りを含めて 11） |
| 6 名 | 15〜16 | 7〜9（ただし依存の直列が先に頭打ちになる） |

**6 名の行に注意すること。** [product-backlog §3.4](product-backlog.md) のとおり、
買取（R3）のように業務そのものが直列な区間では、人を足しても並行にできない。
**人数はベロシティの上限を決めるが、依存はその下限を決める。**

## 8. 準備完了の定義（DoR）

スプリントに引き込んでよい PBI の条件。

- 対象の `STORY-` と `EPIC-` が特定されている（基盤 PBI は解錠する PBI が挙がっている）
- 受け入れ基準がフェーズの指針に沿って書かれている
- 依存する PBI が完了しているか、同一スプリント内で先に完了する見込みがある
- この PBI を止める優先度 **A** の `Q-` が残っていない（残っているなら合意 PBI が先に来る）
- サイズが **L 以下**である

## 9. 完了の定義（DoD）

すべての PBI に共通する条件。個別の受け入れ基準はこれに**上乗せ**される。

- 受け入れ基準がすべて満たされ、各基準について**誰がどう確かめたか**（自動テスト／手動／読み合わせ）が言える
- 触れた `spec/`・`design/` の記述が、実装と食い違っていない
- 自動テストが CI で通り、増えた範囲が [verified-by-test.md](../docs/design/domain/verified-by-test.md) の `SPEC-` に反映されている
- 作業中に判明した未決は `Q-`、モデルの歪みは `ISSUE-` として起票されている（**残すこと自体は完了を妨げない**）
- 顧客 PO が受け入れた（合意 PBI は「合意できた」ことをもって受け入れとする）

## 10. 読み方

```
今スプリントに何を引くか   → product-backlog.md の上から
なぜこの順番か             → product-backlog.md §3
なぜこの粒度か             → policies/splitting.md
受け入れ基準をどう書くか   → policies/acceptance-criteria.md
この PBI の中身            → items/ の該当リリース
```
