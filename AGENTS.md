# AGENTS.md — AI Agent Guidelines & Execution Rules

本プロジェクトの設計課題解決タスクを担当するAIエージェント向けの行動規律・運用ルールです。
コスト（トークン消費）の最小化と、人間レビュー者の負担軽減を最重視して動作してください。

---

## 0. 設計課題定義

docs\snapshot-398efed\14-design-issues.md を参照。

## 1. ブランチ戦略と命名規則

作業ブランチは単一のISSUEごとではなく、依存関係のある課題をまとめた**「Phase単位（中粒度）」**で作成・作業します。

### 開発のベースブランチ

`fix/base` をベースにして、ここを起点にブランチ作成し、PRとマージを積み重ねる。

### ブランチ命名ルール
`fix/p<Phase番号>-<概要>` または `refactor/p<Phase番号>-<概要>`

### ロードマップと対象ブランチ

| Phase | ブランチ名 | 対象ISSUE | 主な解決内容 |
| :--- | :--- | :--- | :--- |
| **Phase 1** | `fix/p1-price-calculation` | ISSUE-12, ISSUE-02, ISSUE-17 | 明細重複の解消、数量を考慮した小計計算、注文時価格の確定 |
| **Phase 2** | `fix/p2-inventory-and-state` | ISSUE-03, ISSUE-15, ISSUE-16 | 在庫超過チェック、状態遷移の導入、キャンセル時の在庫返却 |
| **Phase 3** | `fix/p3-ux-and-security` | ISSUE-11, ISSUE-21, ISSUE-13 | 注文成立時の在庫切れ制御、オファー所有権の検証、対象不在時挙動の統一 |
| **Phase 4** | `fix/p4-business-domain` | ISSUE-06, ISSUE-25 | 買取オファーの在庫化・仕入追跡、期限超過注文のドメイン定義 |
| **Phase 5** | `refactor/p5-model-purity` | ISSUE-07, ISSUE-23, ISSUE-26 等 | 金額・数量の値オブジェクト化、作業単位の明示、モデルの純化 |

---

## 2. 開発・実行手順（作業の基本フロー）

各Phaseの作業を進める際は、必ず以下の手順を遵守してください。

### ① 挙動のテスト固定（最優先）
コード修正を行う前に、**必ず現状の挙動を再現・検証する自動テスト**を追加してください。
* 修正によって既存の挙動が変わる場合（仕様変更）、テストの変更意図を明確に記録します。
* テストを実行し、グリーンであることを確認してから修正に着手します。

### ② こまめな小単位コミット
1つのPhase内で課題（ISSUE）を解決するごとにコミットを作成してください。手戻り時のトークン再消費を削減します。

* コミットメッセージ形式: `fix(<ドメイン領域>): resolve ISSUE-XX <簡潔な概要>`
* 例: `fix(cart): resolve ISSUE-12 merge duplicate cart items`

### ③ コスト最小化の原則（トークン節約）
* 関連性の低いファイルの全件読み込みや、冗長な思考プロセスの出力を避けてください。
* 変更コードのみを的確に編集し、差分を最小限に留めてください。

---

## 3. Pull Request (PR) 作成時のフォーマット

作業完了後、PRを作成します。人間に渡すPRの本文には必ず以下のテンプレートを用いて概要を出力してください。

```markdown
## 概要
Phase <番号> (<ブランチ名>) の実装完了報告

## 解決したISSUE
- [x] ISSUE-XX: <課題名>
- [x] ISSUE-YY: <課題名>

## 実施した主な変更
- <変更点1>
- <変更点2>

## テスト結果・確認事項
- [x] 現状挙動のテスト作成と通過確認
- [x] 修正後の全自動テスト通過確認 (`npm test` / `pytest` 等)

## 人間レビュー者への特記事項（モデル変更・互換性破壊など）
- <人間に特に確認してほしいポイントがあれば記載>