<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- TargetFramework, ImplicitUsings, Nullable, LangVersion inherited from Directory.Build.props -->
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Bookstore.Domain.Tests" />
    <!-- The plain numbers behind Money and Quantity are mapped and queried by the persistence
         layer, and are not part of the model's public surface. -->
    <InternalsVisibleTo Include="Bookstore.Data" />
  </ItemGroup>

</Project>
